[pytest]
addopts = -m "not integration"
markers =
    integration: requires a live 4mica core API
    timeout: per-test timeout in seconds (requires pytest-timeout)
filterwarnings =
    ignore:enable_cleanup_closed ignored because .* is fixed in Python version.*:DeprecationWarning:aiohttp\.connector
